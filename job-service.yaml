spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8079/realms/spring-auth
  datasource:
    url: jdbc:postgresql://localhost:5442/job_db
    username: ${global.database.job-service.username}
    password: ${global.database.job-service.password}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    baseline-version: 0
    baseline-on-migrate: true
    url: jdbc:postgresql://localhost:5442/job_db
    user: ${global.database.job-service.username}
    password: ${global.database.job-service.password}
    baseline-description: "init"
    enabled: true
  rabbitmq:
    username: ${global.rabbitmq.username}
    password: ${global.rabbitmq.password}
    port: ${global.rabbitmq.port}
    host: ${global.rabbitmq.host}
eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/
server:
  port: 8084


